package messages

const (
	UnavailableDatabaseErr = "the database seems to be unavailable right now"
	InsertionErr           = "something went wrong while inserting the received data"
	FetchErr               = "an error occurred while fetching the required data"
	UpdateErr              = "something went wrong while updating the required data"
	TransactionErr         = "a transaction could not be correctly handled"
)
